<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>com.github.reedoverflow.stage1streader</id>
    <name>stage1st-reader</name>
    <vendor>reed-overflow</vendor>

    <depends>com.intellij.modules.platform</depends>

    <!-- 可以配置侧边的工具栏，例如maven所在的侧边栏 -->
    <extensions defaultExtensionNs="com.intellij">
        <!-- 配置侧边工具栏，当且仅当第一次点击时，会初始化窗口类，anchor指定工具栏的位置 -->
        <toolWindow id="S1 Selector" factoryClass="com.github.reedoverflow.stage1streader.window.ForumWindowFactory" secondary="false"
                    anchor="left" icon="AllIcons.Debugger.Db_watch">
        </toolWindow>

        <toolWindow id="S1 Reader" factoryClass="com.github.reedoverflow.stage1streader.window.ThreadWindowFactory" secondary="false"
                    anchor="bottom" icon="AllIcons.Nodes.Folder">
        </toolWindow>
    </extensions>
    
</idea-plugin>
